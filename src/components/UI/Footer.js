import React from 'react'
import { Card } from 'react-bootstrap'

const Footer = () => {
  return (
    <footer className='mt-auto'>
    <Card className="text-center">
      <Card.Footer className="text-muted">Shop</Card.Footer>
    </Card>
    </footer>
  );
}
export default Footer